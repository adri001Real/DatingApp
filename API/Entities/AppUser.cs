namespace API.Entities
{
    public class AppUser
    {
        public int Id { get; set; }//estos get y set obtnert y poner algo de la clase en este caso para este atributo id
        public string UserName { get; set; } //el valor de una varible string es requerido pero si lo queremos hacer opcional hay que poner ? para decir que esa variable es opcional

    }
}